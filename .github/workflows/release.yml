name: Release to Zenodo
on:
  release:
    types: [created]
jobs:
  zenodo:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    # Zenodo integration is handled via .zenodo.json + GitHub app
    - name: Notify
      run: echo 'Release triggered - Zenodo will archive via integration'